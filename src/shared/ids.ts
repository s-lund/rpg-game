/** Stable ID types shared across layers. */

export type EntityId = `ent_${string}`;
export type SiteId = `site_${string}`;
export type EncounterId = `enc_${string}`;
export type BeatId = `beat_${string}`;
export type AssetId = string;
